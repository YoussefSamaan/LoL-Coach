[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ml"
version = "0.1.0"
description = "Offline feature building and artifact training"
dependencies = [
    "core",
    "pandas",
    "numpy",
]

[tool.setuptools.packages.find]
where = ["src"]
