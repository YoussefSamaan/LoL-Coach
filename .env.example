# Copy to .env and fill values (this file is safe to commit).
# Format: `RGAPI-...` (dev keys expire; regenerate if you get HTTP 403).
# Required for data extraction/ingestion pipelines.
RIOT_API_KEY=RGAPI-your-dev-key-here

# Required for explanation generation.
# Set GENAI_PROVIDER to 'gemini' (default) or 'openai'
GENAI_PROVIDER=gemini
GEMINI_API_KEY=your-gemini-api-key-here
OPENAI_API_KEY=your-openai-api-key-here
